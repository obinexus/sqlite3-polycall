# dist

Runtime SQLite databases can be written here by downstream packaging workflows.

The npm package includes this directory for indexability, but generated `.db`,
`.sqlite`, and `.sqlite3` files are ignored by git.
